# Judgements for Johannesburg, target 2021.
#
# These parties will be on the ballot and did not contest the election
# the pool weights were fitted on, so nothing measured says who votes
# for them. THE MODEL CANNOT ANSWER THIS. You can.
#
# TWO QUESTIONS PER PARTY:
#
#   1. WHICH POOLS DOES IT PULL FROM?
#      Set `parent` and it inherits that party's pool weights — which
#      is what a splinter is. It then takes its votes OUT OF THOSE
#      POOLS, and every party holding them gives up the same
#      proportion of what it held. No party is singled out: the parent
#      loses most only because it holds most.
#      Or set `weights` directly, in the order
#        Black African, Coloured, Indian/Asian, White
#      normalised, so [0, 0, 0, 1] means 'entirely the last pool'.
#      Leave both unset and it is spread like the city: each pool
#      weighted by its share of the city's votes. That is a default,
#      not a measurement. If you know where this party draws from, say.
#
#   2. HOW MUCH SUPPORT DO YOU EXPECT?
#      `support` is its share of the city. The default is what
#      arrivals have historically won per ward contested, times the
#      wards this one is contesting — so it rises with reach and falls
#      without it. Override when you know something the record does
#      not: ActionSA in 2021 was led by a popular former mayor of this
#      city and took 18.12%, against a default near a third of one per
#      cent. Nothing measurable could have said so; a person could.
#
# Every override is recorded in the run's provenance, and should be
# deleted once the election has been held and the weights measured.

[party.GOOD]
baseline_share = 0.0031
parent = ""      # e.g. "ANC" to inherit the ANC's pool vector
# weights = [0.0, 0.0, 0.0, 0.0]

[party.ATM]
baseline_share = 0.0026
parent = ""      # e.g. "ANC" to inherit the ANC's pool vector
# weights = [0.0, 0.0, 0.0, 0.0]

[party.BLACK_FIRST_LAND_FIRST]
baseline_share = 0.0018
parent = ""      # e.g. "ANC" to inherit the ANC's pool vector
# weights = [0.0, 0.0, 0.0, 0.0]

[party.NFP]
baseline_share = 0.0009
parent = ""      # e.g. "ANC" to inherit the ANC's pool vector
# weights = [0.0, 0.0, 0.0, 0.0]

[party.ECONOMIC_EMANCIPATION_FORUM]
baseline_share = 0.0003
parent = ""      # e.g. "ANC" to inherit the ANC's pool vector
# weights = [0.0, 0.0, 0.0, 0.0]

[party.NATIONAL_PEOPLE_S_FRONT]
baseline_share = 0.0003
parent = ""      # e.g. "ANC" to inherit the ANC's pool vector
# weights = [0.0, 0.0, 0.0, 0.0]

[party.AFRICAN_COVENANT]
baseline_share = 0.0003
parent = ""      # e.g. "ANC" to inherit the ANC's pool vector
# weights = [0.0, 0.0, 0.0, 0.0]

[party.AFRICAN_PEOPLE_S_MOVEMENT]
baseline_share = 0.0000
parent = ""      # e.g. "ANC" to inherit the ANC's pool vector
# weights = [0.0, 0.0, 0.0, 0.0]

[party.PEOPLE_S_FREEDOM_PARTY]
baseline_share = 0.0000
parent = ""      # e.g. "ANC" to inherit the ANC's pool vector
# weights = [0.0, 0.0, 0.0, 0.0]

[party.ABANTU_BATHO_CONGRESS]
baseline_share = 0.0000
parent = ""      # e.g. "ANC" to inherit the ANC's pool vector
# weights = [0.0, 0.0, 0.0, 0.0]

[party.US_THE_PEOPLE]
baseline_share = 0.0000
parent = ""      # e.g. "ANC" to inherit the ANC's pool vector
# weights = [0.0, 0.0, 0.0, 0.0]

[party.DOP]
baseline_share = 0.0000
parent = ""      # e.g. "ANC" to inherit the ANC's pool vector
# weights = [0.0, 0.0, 0.0, 0.0]

[party.ROYAL_LOYAL_PROGRESS]
baseline_share = 0.0000
parent = ""      # e.g. "ANC" to inherit the ANC's pool vector
# weights = [0.0, 0.0, 0.0, 0.0]

[party.DEMOCRATIC_ARTISTS_PARTY]
baseline_share = 0.0000
parent = ""      # e.g. "ANC" to inherit the ANC's pool vector
# weights = [0.0, 0.0, 0.0, 0.0]

[party.AGENCY_FOR_NEW_AGENDA]
baseline_share = 0.0000
parent = ""      # e.g. "ANC" to inherit the ANC's pool vector
# weights = [0.0, 0.0, 0.0, 0.0]

[party.UNITED_CULTURAL_MOVEMENT]
baseline_share = 0.0000
parent = ""      # e.g. "ANC" to inherit the ANC's pool vector
# weights = [0.0, 0.0, 0.0, 0.0]

[party.AFRICAN_FREEDOM_REVOLUTION]
baseline_share = 0.0000
parent = ""      # e.g. "ANC" to inherit the ANC's pool vector
# weights = [0.0, 0.0, 0.0, 0.0]

[party.BLACK_AND_WHITE_PARTY]
baseline_share = 0.0000
parent = ""      # e.g. "ANC" to inherit the ANC's pool vector
# weights = [0.0, 0.0, 0.0, 0.0]

[party.AFRICAN_AMBASSADORS_OF_SOUTH_AFRICA]
baseline_share = 0.0000
parent = ""      # e.g. "ANC" to inherit the ANC's pool vector
# weights = [0.0, 0.0, 0.0, 0.0]

[party.ACTIVISTS_MOVEMENT_OF_SOUTH_AFRICA]
baseline_share = 0.0000
parent = ""      # e.g. "ANC" to inherit the ANC's pool vector
# weights = [0.0, 0.0, 0.0, 0.0]

[party.AFRICA_RESTORATION_ALLIANCE]
baseline_share = 0.0000
parent = ""      # e.g. "ANC" to inherit the ANC's pool vector
# weights = [0.0, 0.0, 0.0, 0.0]

[party.AFRICAN_SECURITY_CONGRESS]
baseline_share = 0.0000
parent = ""      # e.g. "ANC" to inherit the ANC's pool vector
# weights = [0.0, 0.0, 0.0, 0.0]

[party.SHOSHOLOZA_PROGRESSIVE_PARTY]
baseline_share = 0.0000
parent = ""      # e.g. "ANC" to inherit the ANC's pool vector
# weights = [0.0, 0.0, 0.0, 0.0]

[party.SAKHISIZWE_CONVENTION]
baseline_share = 0.0000
parent = ""      # e.g. "ANC" to inherit the ANC's pool vector
# weights = [0.0, 0.0, 0.0, 0.0]

[party.INTERNATIONAL_PARTY]
baseline_share = 0.0000
parent = ""      # e.g. "ANC" to inherit the ANC's pool vector
# weights = [0.0, 0.0, 0.0, 0.0]

[party.FORUM_4_SERVICE_DELIVERY]
baseline_share = 0.0000
parent = ""      # e.g. "ANC" to inherit the ANC's pool vector
# weights = [0.0, 0.0, 0.0, 0.0]

[party.AHC]
baseline_share = 0.0000
parent = ""      # e.g. "ANC" to inherit the ANC's pool vector
# weights = [0.0, 0.0, 0.0, 0.0]

[party.PARTY_OF_ACTION]
baseline_share = 0.0000
parent = ""      # e.g. "ANC" to inherit the ANC's pool vector
# weights = [0.0, 0.0, 0.0, 0.0]

[party.CIVIC_MOVEMENT_OF_SOUTH_AFRICA]
baseline_share = 0.0000
parent = ""      # e.g. "ANC" to inherit the ANC's pool vector
# weights = [0.0, 0.0, 0.0, 0.0]

[party.COMMUNITY_SOLIDARITY_ASSOCIATION]
baseline_share = 0.0000
parent = ""      # e.g. "ANC" to inherit the ANC's pool vector
# weights = [0.0, 0.0, 0.0, 0.0]

[party.SOUTH_AFRICAN_ROYAL_KINGDOMS_ORGANIZATION]
baseline_share = 0.0000
parent = ""      # e.g. "ANC" to inherit the ANC's pool vector
# weights = [0.0, 0.0, 0.0, 0.0]

[party.UIM]
baseline_share = 0.0000
parent = ""      # e.g. "ANC" to inherit the ANC's pool vector
# weights = [0.0, 0.0, 0.0, 0.0]

[party.AMALGAMATED_RAINBOW_MOVEMENT]
baseline_share = 0.0000
parent = ""      # e.g. "ANC" to inherit the ANC's pool vector
# weights = [0.0, 0.0, 0.0, 0.0]

[party.ASA]
baseline_share = 0.0000
parent = ""      # e.g. "ANC" to inherit the ANC's pool vector
# weights = [0.0, 0.0, 0.0, 0.0]

[party.THE_ORGANIC_HUMANITY_MOVEMENT]
baseline_share = 0.0000
parent = ""      # e.g. "ANC" to inherit the ANC's pool vector
# weights = [0.0, 0.0, 0.0, 0.0]

[party.JUSTICE_AND_EMPLOYMENT_PARTY]
baseline_share = 0.0000
parent = ""      # e.g. "ANC" to inherit the ANC's pool vector
# weights = [0.0, 0.0, 0.0, 0.0]

[party.ABAHLALY_BAAHI]
baseline_share = 0.0000
parent = ""      # e.g. "ANC" to inherit the ANC's pool vector
# weights = [0.0, 0.0, 0.0, 0.0]

[party.CHANGE]
baseline_share = 0.0000
parent = ""      # e.g. "ANC" to inherit the ANC's pool vector
# weights = [0.0, 0.0, 0.0, 0.0]

[party.DISRUPT_PARTY]
baseline_share = 0.0000
parent = ""      # e.g. "ANC" to inherit the ANC's pool vector
# weights = [0.0, 0.0, 0.0, 0.0]
